(function ($) {
    "use strict";

    ///////////////////////////////////////////////////// Your
    // var venueAddress = "Grand Place, 1000, Brussels"; // Venue

    // Map Object
    var map = null;
    var europe;
    // europe
    <%= partial js_dir + "/javascript-unit-test-leaflet-01/partials/_var_europe" %>



    /////////////////////////////////////////////////// Adress

    var fn = {

        // Launch Functions
        Launch: function () {
            fn.Apps();
            fn.LeafLet();
        },

        // Leaflet
        <%= partial js_dir + "/javascript-unit-test-leaflet-01/partials/_fn_LeafLet" %>
        // Apps
        <%= partial js_dir + "/javascript-unit-test-leaflet-01/partials/_fn_Apps" %>

    };

    $(document).ready(function () {
        fn.Launch();
    });

})(jQuery);